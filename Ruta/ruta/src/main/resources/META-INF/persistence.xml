<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="UsuariosUP" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>java\entities\User.java</class>

        <properties>
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/ruta" />
            <property name="javax.persistence.jdbc.user" value="root" />
            <property name="javax.persistence.jdbc.password" value="password" />
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver" />
            <!-- Cambios realizados -->
            <!-- Eliminamos la propiedad para evitar la eliminación y recreación de la base de datos -->
            <!-- Cambiamos hibernate.hbm2ddl.auto a validate para validar el esquema sin modificar la base de datos -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect" />
             <!-- Estrategia de creación de tablas -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            
            <!-- Nombre de la base de datos -->
            <property name="hibernate.default_schema" value="ruta"/>
            
            <!-- Configuraciones adicionales de Hibernate -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

</persistence>
